/**
 * Code généré via SNIPPET (https://docs.microsoft.com/fr-fr/visualstudio/ide/walkthrough-creating-a-code-snippet?view=vs-2019)
 * Type "impRSL", Press Tab, replace the "TO_REPLACE" word by your class name, and done.
 */

import React from 'react';

/*
// A utiliser si on a besoin de faire des actions avant de renvoyer le rendu (ex : placer du JS, logique, ...)
const titreH2 = (props) => {
    // Placer du JS ici si besoin

    // Resultat final incluant le JS
    return (
        
    );
};
*/

const titreH2 = (props) => (

);

export default titreH2;
